# GEOly MCP — full tool catalog (up to 82 tools)

_Use this when you need a tool's exact parameters/enums/defaults, or to confirm whether a tool is exposed over MCP._

Ground-truth catalog of the tools the GEOly MCP server **registers**. This is the MCP surface
— several internal agent tools exist in the codebase but are NOT exposed over MCP (see
§ Not on MCP). Availability depends on **mode** and **plan**:

- **Discovery selectors** (`list_organizations`, `list_brands`) appear only in multi-brand /
  multi-org mode.
- **Write tools** require write access granted on the OAuth consent screen (per-resource
  read/write grid; legacy `?tool_profile=standard` still honored); read-only by default, and
  multi-org connections are **always** read-only (write grants clamped).
- **Public tools** require a Grow-tier-or-above plan; multi-org connections get them when
  **any** accessible org qualifies.
- Brand-scoped tools auto-resolve the brand when the org has one brand or the token is
  brand-bound; otherwise pass `brand_id` (discover it via `list_brands`).

Parameter notation: `name: type (constraints, default)`. `time_range` is the shared enum
`7d | 30d | 90d` (default `30d`) **unless the row says otherwise**.

---

## A. Discovery & routing (6)

| Tool | Purpose | Params |
|---|---|---|
| `list_organizations` | List org IDs the token can access (multi-org mode only) | — |
| `list_brands` | List brand IDs in the org (multi-brand / multi-org only) | `org_id` (optional, multi-org only) |
| `get_brand_context` | **Call first, once per run (free).** One-shot orientation: `brand` (id, name, domain, website + the Settings › Brand profile fields `industry`, `description`, `logo_url`), `organization` (id, name, plan, subscription_status), `integrations` (`ga4` / `cloudflare` — the /sources/traffic readiness flags; `ga4=false` ⇒ `get_ga4_traffic_data` returns `NO_PROPERTY_BOUND`), `today` (`utc_date`, `business_date` in Asia/Shanghai, `record_date_key` — the date-axis value today's records land under, one day behind the business day), `first_run` (`phase` collecting / failed / ready / none; `progress` counts only for collecting / failed — a new brand's empty window is not zero visibility), `platforms` (the plan's entitled platforms — the in-app /performance chips, `entitled=true`, `completed_records_30d` may be 0 — plus any platform with completed records in the last 30 days, `entitled=false` = collected under an earlier plan: code, name, `entitled`, `last_record_date`, `completed_records_30d`), `topics` (id, name, promptCount), `scope_universe` (`countries`, `has_ungrouped_prompts`, `ungrouped_topic_id` — the value set the page's topic / country filters accept; **only `get_brand_board` validates `country`** against it (`error: "unknown_country"`) — `list_brand_answers` does not, an unknown code just yields an empty list), `competitors` (**entity-layer brand library, tracked competitors only** — same rows as `get_competitor_list status="tracked"` minus the own-brand row: `entity_id`, `name`, `root_domain`, `mentions_30d` + legacy `id` / `domains` / `aliases` (**user-added spellings only** — `get_competitor_list`'s same-named field lists *all* spellings, so don't diff the two) / `isActive`) with `competitor_counts` {tracked, suggested, removed}, `data_window` (first/last record_date), `credits` (remaining AI / MCP). After it, do not call `get_current_date`, `get_topic_list` or `get_available_platforms` again in the same run, and do not call `get_competitor_list` just to list tracked competitors — still call it for suggestions, removed brands or learned spellings. No KPI numbers — those stay in `get_brand_overview` | — |
| `get_current_date` | Server time, for date-range validation (superseded by `get_brand_context.today` in normal runs) | — |
| `get_quota` | Quota status for the token's org(s): monthly MCP credits used / remaining, period reset date, enforcement mode. Always registered | — |
| `resolve_page_context` | Desktop client page awareness: resolve the GEOly web-app URL the user is viewing into an entity scope — pageKind, entity id + title, effective params, suggested tools | `url` |

---

## B. Brand-own — overview & KPI (read-only)

| Tool | Purpose | Params |
|---|---|---|
| `get_brand_overview` | **KPI headline** = the in-app `/performance` KPI row, all five cells: `aigvr.score` (Visibility), `aigvr.mentionRate` (Mention rate), `shareOfVoice.share` (Share of voice, legacy discovered-brand caliber — `null` + `shareOfVoiceState:"not-ready"` when the derived daily layer isn't ready), `bestPlatform` / `worstPlatform`. Plus tool-only `citationRate`, `keyMetrics.totalMentions` / `avgPosition` and `platformStats[]` (`aigvrScore` / `mentionRate` / `citationRate` = that platform's KPI cells, **`null` when the platform had no completed records** — null means "not run", never rank it worst; `completed` / `avgPosition` / `totalMentions` count all statuses). Echoes `window {timeRange,start,end,endBounded}` as **full ISO instants** (a `custom` +08 business day starts at `T16:00:00Z` the day before — quote the business days you asked for, never the sliced UTC date; `start_date`/`end_date` are read only with `time_range=custom`, otherwise ignored and `window.note` says so), `platform`, `scope`, `kpiState` (`unavailable` = a filtered KPI query blew its 10s budget ⇒ no numbers at all). `hasCompletedRecords=false` / `aigvr.totalCount=0` = nothing ran in that window/scope: the scores are 0 placeholders (`_message` says so), never "0% visibility" | `time_range (7d/30d/90d/180d/custom), start_date, end_date, platform (all | one of the 6 platform codes — closed enum), topic_ids[] (≤50, each id 1–64 chars), country (ISO-2, ≤8 chars)` |
| `query_analytics` | Controlled aggregation (no SQL) over daily datasets — also the **daily trend** tool (dataset `brand_citations_daily`: aigvr / mentionRate / citationRate per day per platform with the `completedRecords` denominator; `citationCount` = raw citation URLs that day — a **different numerator** from `citationRate`, never divide one by the other). `date` is a **UTC** day key = in-app business day − 1 (add the `businessDate` dimension for the app's label); `topic_ids[]` / `country` reproduce the `/performance` filter chips | see **§ query_analytics** below |
| `resolve_my_brand_public` | Bridge: resolve this monitored brand → its `public_brand`. `bestMatch` is the **same decision the in-app /performance industry-profile card makes** (domain root authoritative → exact normalized name / match terms → alias; single active data-bearing entity only) and carries `availableLocales`; `matched ⇔ bestMatch !== null`; `ambiguous=true` = several active public brands collide (fail-closed, `bestMatch=null`). `candidates` (≤5, `matchedBy: link\|domain\|name\|alias` — heuristic rows are link/domain/name; when matched `candidates[0]` is `bestMatch` with its own domain/name/alias) is a heuristic context list — never substitute `candidates[0]` for a null `bestMatch` | — |

### query_analytics
- `dataset`: `brand_citations_daily | topic_citations_daily | topic_domain_citations_daily`
- `start_date, end_date` (YYYY-MM-DD; range ≤ 366 days)
- `dimensions[]`: `date | businessDate | platform | platformName | topicId | topicName | domain` (`platformName` = display name, pairs with `platform`; `businessDate` = the UTC+8 business-day label the app shows, pairs with `date`)
- **Calendar**: `date` (and `start_date` / `end_date`) is `record_date` truncated to a **UTC** day key = the in-app business day **minus one day** — a row labelled `date="2026-09-21"` is what `/performance` plots on 2026-09-22. Add `businessDate` when you quote dates to a human. Daily `aigvr` / `mentionRate` / `citationRate` carry 1 decimal, same as the in-app trend chart.
- `metrics[]`: `citationCount | citedRecords | mentionCount | mentionedRecords | completedRecords | brandCitationRecords | aigvr | mentionRate | citationRate | recordCitationRate | sentiment.*` (`sentiment.positive/neutral/negative/mixed/unknown/scoredRecords/avgScore`)
- `platform`, `topic_id`, `topic_name`, `domain` (filters). `topic_id` takes **one** topic (`topic_citations_daily` / `topic_domain_citations_daily`) and accepts `"_none"` (legacy alias `"_ungrouped"`) for prompts without a topic
- `topic_ids[]` (1–50 ids, each id 1–64 chars), `country` (ISO-2, ≤8 chars) — **`brand_citations_daily` only** (any other dataset rejects them): the `/performance` topic chips (multi-select, `"_none"` = prompts without a topic) and country chip (ISO-2), so the daily series matches the **filtered** trend chart. Validated against the brand like the page does: unknown topic ids / countries are rejected (never silently dropped), and so are blank values / an empty list — **omit** the parameter for "no filter". Selecting every topic — or the only country a brand has — counts as no filter, and the response then echoes `filters.topicIds=null` / `filters.country=null` + `filters.scopeNote` so you can tell a folded filter from a real one
- `prompt_text_include`, `prompt_text_exclude` — **`topic_citations_daily` only**; define a prompt subset by text (live today)
- `limit`: 1–1000
- `compare_previous` (bool, default false): also runs the identical query over the equal-length window immediately before `start_date`; every row gains `previous.<metric>` and `delta.<metric>` (current − previous; rates are percentages, so the delta is in percentage points), and the response carries `window.{current,previous}.{start,end,days,days_with_data}` — read `days_with_data` before quoting a delta (a 7-day window with 6 collection days is not like-for-like). Rows with the `date` dimension are aligned by position (day i vs day i); `previous.date` shows the day actually compared. Price unchanged (per call).
- Returns `{ rows[], totalRows, _ref }` — `_ref` is for the in-app agent's display tools; MCP
  callers receive plain JSON and do not get a usable `_ref` (display tools aren't on MCP).
- **Daily trend recipe** (what `get_brand_citations_daily` used to return): `dataset=brand_citations_daily`,
  `dimensions=["date","platform"]` (add `"platformName"` for display names), `metrics=[citationCount,
  mentionCount, mentionedRecords, completedRecords, brandCitationRecords, aigvr, mentionRate,
  citationRate, sentiment.*]`. A row exists only for (date, platform) pairs with completed
  monitoring records — a missing day means no monitoring ran, not 0.

---

## C. Brand-own — prompts (read-only)

| Tool | Purpose | Params |
|---|---|---|
| `get_prompt_list` | Search/list prompts with the **same read model and calibers as the `/prompts` table** (2026-09-20). Returns `{ prompts[], summary, totalRows, totalPages, currentPage, pageSize, competitorsIncluded }`. Per row: `geoMetrics.aigvr.{score, mentionRate (= ranked/completed), citationRate (= brand-cited/completed, headline caliber), rankedCount, totalCount}`, `delta.{visibility, position, citationRate}`, `platformRecords`, `performance`. `summary` = the bar above the table (whole filtered set, record-weighted `visibility` / `mentionRate` / `citationRate` + `*Delta` vs the previous equal window). **Competitors / SoM are opt-in**: default `include_competitors=false` (the page's first screen) → rows carry `competitorsDeferred: true` and have **no** `som` / `competitorMentions` / `delta.share`; `include_competitors=true` → real per-prompt Share of Mentions (`som.{share, rank, totalCompetitors}`, top-5 `competitorMentions` with `share`) — slower. `status` picks the page tab (`active` default / `archived`; no "all"). Truncation: `prompts` is **always an array**; if the page was cut, top-level `_truncated` / `_totalCount` (rows on this page before the cut) / `_shownCount` / `_message`. `citationsCount` (citation URLs in window) is tool-only. **All filters are ANDed like the page** — `search` no longer drops `topic` / `tags` (it used to, silently widening to the whole brand), and it matches prompt text **or tag name**. Rows also carry `topicId`, `archivedAt`, `isBranded` (the page's "type" column) and `inFlightCount` (records still pending/running in the window, <14h old). Unknown `country` ⇒ error object with `countryOptions[]` instead of a silently empty page; `countryOptions[]` is echoed whenever `country` is passed or the brand spans >1 country | `page, page_size (1–100; ~45 full rows fit the output cap — check `_truncated`), compact (bool: slim rows so 100/page fit — use for enumerating / exporting), search (prompt text OR tag name), sort_by: text\|visibility\|position\|citations\|date\|archivedAt (omit ⇒ archivedAt on status=archived, date otherwise; position is tool-only), sort_order: asc\|desc, time_range, start_date, end_date, platform, tags[], topic[], topic_name, status: active\|archived, country, include_competitors (bool, default false)` |
| `get_prompt_detail` | One prompt with the **same read model as `/prompts/[id]`** (2026-09-20): **windowed** (default 30d; `custom` clamped to 90d) and platform-scoped (unknown / un-entitled platform → error object). Returns `{ prompt, window, overview, sourceDomains }`: `overview.{totalRecords, mentionedRecords (position IS NOT NULL), mentionRate, brandVisibilityScore (per-day AIGVR averaged over days — overview caliber, ≠ the record-weighted list-row score), brandShare (Share of Mentions vs entity-layer competitors), topCompetitor, discoveredCompetitorCount, benchmarkCompetitors (top 4), visibilityBoard[] (**competitors only — your own brand is never a row**), brandRank (where you sit in that board = rows beating `brandVisibilityScore` + 1; **`null` when the window has no completed records** — the page draws no board or rank then; a lower bound once `visibilityBoardTotal > visibilityBoard.length`), benchmarkTrend[] (one point per `trend_granularity` bucket; with week/month `date` is the bucket start — ISO Monday / the 1st), trendGranularity (echo), platformMatrix (≥ 2 platforms only)}`; `sourceDomains.{rows (cited root domains, **one page of 10** — `sources_page` walks the rest), page, pageSize, totalDomainCount (⇒ pages = ceil / 10), breakdown {total, brand, competitor} (window-wide, same on every page), ownDomains}`. `include_lifetime=true` adds the **deprecated** old payload under `lifetime` (all-time, all-platform `recordsCount / citationsCount / geoMetrics / platformRecords`; removal scheduled) | `prompt_id, time_range: 7d\|30d\|custom, start_date, end_date, platform (default all), sources_page (int 1–1000, default 1), trend_granularity: day\|week\|month (default day; the page itself defaults to week on 30d/custom), include_lifetime (bool, default false)` |
| `get_prompt_record_summaries` | **Latest** monitoring record per platform for one prompt (one row per platform, no history). Rows carry `recordDate` (stored run timestamp) **and** `businessDay` (UTC+8 business day `YYYY-MM-DD`, the date the app shows — quote this one) | `prompt_id` |
| `list_prompt_records` | **Full execution history** of one prompt over a time range, paginated — every daily record, not just the latest. Each row: `id, platformCode/Name, recordDate, businessDay, status, position, mentionRank, mentions, sentiment, sentimentScore, shoppingVisible`. `businessDay` = UTC+8 business day `YYYY-MM-DD` (the app's date axis; `recordDate` stores business day D as `(D−1)T16:00Z`). `shoppingVisible` is a per-answer flag: ChatGPT = upstream `shopping_visible`; Google AI Mode = derived from captured shelf cards, so **true or null there, never false**; null on Perplexity / Gemini / Copilot / AI Overview — the `/performance/shopping` page's trigger rate / trend use "answers with ≥ 1 captured shelf card" as the denominator, which can differ. Feed a returned `id` to `get_prompt_record_detail`. Use for per-day trend analysis: list here, then pull detail only for the records you need. Unknown / un-entitled `platform` → error object (2026-09-22; was a silent `total=0`) | `prompt_id, platform (all \| one entitled code incl. copilot), time_range (7d\|30d\|90d, default 30d), start_date, end_date (explicit dates = UTC+8 business days, must be paired; they override time_range), limit (1–200, default 100), offset, sort_order: asc\|desc` |
| `list_brand_answers` | **Brand-level answers table** (= in-app `/performance/answers`): every completed AI answer for the brand in the window across ALL prompts, newest first, paginated. Filters: `platform`, `topic_ids`, `tags` (tag **names**; `_none` = untagged), `country` (ISO2), `entity_id` (answers mentioning one brand entity), `only_mentioned`. Row: `recordId, promptId, promptText, platformCode/Name, recordDate, businessDay (UTC+8 business day the page shows — quote this as the date), country, brandMentioned, mentions, answerSummary, entities[] {entityId, displayName, isOwnBrand, domain}, entityTotal, sources[] (root domains), sourceTotal, citationCount, features {web, shopping, map, aiOverview}`; the echoed `window` also carries `startBusinessDay` / `endBusinessDay` = the **first / last business day the window actually covers** (`endBusinessDay` `null` = up to now; on rolling windows `startBusinessDay` is rounded up to the first matchable day, so it is not `start`'s calendar date). `total` computed on page 1 only; `hasMore` is the pagination truth; `status=filter_too_broad` = narrow the filter (fail-closed, never truncated). No position/sentiment column — drill with `get_prompt_record_detail` | `time_range (7d\|30d\|90d\|custom, default 30d), start_date, end_date (custom; UTC+8 business days, span ≤ 90d), platform, topic_ids[], tags[], country, entity_id, only_mentioned (bool), page (1–100), page_size (1–50, default 20)` |
| `get_prompt_record_detail` | Full detail of one monitoring record — the in-app answer dialog's read model (answer, citations, sentiment, shopping cards). **Dialog calibers (2026-09-22)**: `businessDay` (UTC+8 `YYYY-MM-DD`, the date shown; `recordDate` is the stored `(D−1)T16:00Z` timestamp); `webSearchStatus` = `searched \| not_searched \| unknown` resolved from citations + searchSources + echo-free queries + the flag + `aiModel` — the raw `webSearchTriggered` alone mis-reports `false` ~85% of the time; `realtimeQueries` = the "AI searched for" list (prompt echoes dropped, de-duplicated; `webSearchQuery` is the raw list); `displayed.{sentiment, mentionRank}` = the status-bar values after the dialog's gates (sentiment only when `status=completed` and `mentions > 0`; rank only when additionally another brand entity was named and `mentionRank > 0`; `null` = not shown — the raw `sentiment` / `mentionRank` are ungated); `citations` (≤30) / `searchSources` (≤30) / `shopping` (≤20) are capped but `citationTotal` / `searchSourceTotal` / `shoppingTotal` + `citationsTruncated` / `searchSourcesTruncated` / `shoppingTruncated` give the full counts (the dialog's "N sources" = `citationTotal`); `promptText` included. Brands named in the answer: read **`mentionedEntities`** (2026-09-20; entity layer, same as the answer dialog): `[{ entityId, displayName, isOwnBrand, majorityRole, stance: preferred\|equal\|inferior\|we_absent\|null, stanceSource: vote\|prmb\|null, mentionCount, domain }]`, own brand first. `mentionedBrands` (legacy `prompt_record_mentioned_brand`, raw names, 5th value `neutral`) and `competitors` (legacy text column × user-configured competitors) are **deprecated** and can disagree with `mentionedEntities` | `record_id, include_answer_text (bool, default false)` |
| `get_prompt_citations` | Citation list for a prompt — raw (per-record rows with `date` = stored timestamp **and** `businessDay` = UTC+8 business day; `searchQueries` echo-free) or deduplicated URL list with `share%` (= the `/prompts/[id]` sources "pages" tab, `firstSeen` / `lastSeen` in UTC+8 days). **2026-09-22**: explicit `start_date` / `end_date` are UTC+8 calendar days (Beijing 00:00 → 23:59:59.999, the page's boundaries; was UTC midnight — old pulls with explicit dates shift by 8h); spans up to 366 days still accepted (no 90-day clamp). Unknown / un-entitled `platform` → error object (was: silently read un-entitled history) | `prompt_id, deduplicate (bool), limit (1–500), offset, time_range, start_date, end_date (UTC+8 days, paired), sort_order: asc\|desc, platform (all \| one entitled code incl. copilot), domain` |
| `get_prompt_mention_rates` | Per-prompt mention rate, **ascending by default** — blind-spot discovery ("which queries never mention us"); the first page is *expected* to be zeros. For the most-mentioned prompts pass `sort_order=desc`. Paged over a total order (`offset` + the returned `total`), filterable by `topic_ids` (ids from `get_topic_list` / `get_brand_context`; `topic_id` is a single-id alias merged into it, and the 50-id cap applies to the merged de-duplicated set — over it is an error, never a silent truncation). Primary columns (2026-09-20, `/prompts` caliber): `rankedCount` (completed records with `position IS NOT NULL`) and `rankedRate` (= rankedCount / totalRecords, %). `mentionCount` / `mentionRate` (numerator `mentions > 0`, = `get_brand_overview.mentionedCount`) are **deprecated**; by definition mentionRate ≥ rankedRate (identical in current production data — a predicate alignment, not a number change), never mix them. Response echoes `total, offset, limit, sortBy, sortOrder, topicIds` | `time_range, min_records (1–100), limit (1–100), offset (0–10000), sort_by (rankedRate / mentionRate / totalRecords / rankedCount), sort_order (asc / desc), topic_ids[] (≤50 merged, each id ≤64 chars; topic_id single alias), only_active (bool)` |
| `get_brand_search_queries` | AI search queries (query fanout) for the brand: the real web searches ChatGPT / Perplexity issued while answering tracked prompts, scoped by time window (default 30 days) and optionally platform / topic. `mode=overview` KPI block + who AI verifies by name + which sites it scrapes; `groups` evidence table grouped by prompt; `query_detail` per-query drill-down; `prompt_queries` one prompt's queries **as on `/prompts/[id]`** (2026-09-20): follows `time_range` (default 30d) and `platform` (default `all` = every entitled platform; was a fixed 90-day ChatGPT-only window — reproduce old pulls with `time_range=custom` over 90 days + `platform=chatgpt`), adds `platformCodes[]` per query, `totalQueries`, `echoDistinctCount`, `roots[]` / `competitorRoots[]`; a `prompt_id` that is not this brand's returns an error object (2026-09-22; was an empty `recordsWithQueries=0` shell). **Deprecated modes** (step 1 of retirement: still forwarded, each response carries `_deprecated`, removal scheduled): `roots` -> `overview`, `topic_roots` -> `groups`, and **`root_detail` -> `groups` with `root_key` used as a substring search over query text** (the old root drill returned every query containing the root, so a substring search is the closest match — an exact `query_detail` lookup would miss almost every legacy call). **Page calibers (2026-09-22):** above the sample safety cap `overview` / `groups` return `{ error, truncated: true }` instead of numbers (the page blanks the whole screen — a zero would be a wrong answer, not a small one); a platform that does not expose search queries returns an explicit error without reading anything, and a platform the brand is not entitled to returns a separate "not entitled" error — neither is "no data"; `topic_ids` is the page's multi-select topic filter (`topic_id` kept as the single alias); `groups` rows carry `lastRecordDate` (last business day the query showed up) | `mode (overview, groups, query_detail, prompt_queries), time_range, start_date, end_date, platform, topic_ids, topic_id, normalized_query, prompt_id, page, query_search, type_filter, result_filter` |

| `get_topic_list` | The brand's monitored topics (the /prompts left rail) — `id`, `name`, `description`, `position`, `createdAt`/`updatedAt`, and **two counts per topic**: `activeCount` (still monitored) + `archivedCount`; `promptCount` == `activeCount`. Both matter: a lone `0` cannot tell "never had prompts" from "all of them are archived". Ungrouped prompts are **not** a row here — for the rail's "No topic" counts call `get_prompt_list topic=['_none']` once per `status` and read `totalRows`. **This is where topic ids come from** for `get_topic_analytics.topic_ids`, `query_analytics.topic_id` / `topic_ids`, `get_prompt_list.topic`. Free | — |

> `get_brand_context.topics` also returns every topic's id / name / promptCount in one call.
> On the public surface use `list_public_topics` instead.

---

## D. Brand-own — citations, domains & pages (read-only)

| Tool | Purpose | Params |
|---|---|---|
| `get_citation_overview` | The `/sources/citations` domains tab: `{ caliber, window, stats, board }`. **`board` (0.6.0) = the redesigned page, same read model**: `totals {roots, citations}`, `you {rootDomains, citations, share, rank}` ("your site holds X%, ranks #N"), `movers {top, new, up, down}` + `enteredTop20` / `biggestDrop` (closed days vs the previous equal-length window; `prevCitations`/`deltaPct` null = no comparison — 90d never compares, `prevReady=false` = previous window still building), `trend` (top-5 domains + you per day), `typeShare {you, competitor, other}`, `coverage.missingDays`; `status="not-ready"` is a state, not empty data. `stats` = the older KPI block (counts URLs, not records): totals, ownership, top domains, subdomains, daily trend, platform distribution — kept for existing callers; `stats.trend.citations` (half vs half) is deprecated, use `board.movers`. With `topic_ids` / `competitor_ids` the response is `{ board }` only. Window = N whole +08 calendar days on the citation-creation axis + today; `caliber` (`created-day` / `legacy`) + `window` describe `stats` — quote them. **Precision (0.6.0):** the page shows shares with one decimal — `stats.topDomains[].share` / `stats.platformDistribution[].share` are the raw 0–1 ratio to format (`percentage` is whole-number rounding for old callers); the page's type-share card is `board.typeShare` (`value ÷ total`), not the integer `ownership` points. `platform` must be an **active** platform the brand is entitled to — the page's chips (unknown, un-entitled or retired code → `{ error }` listing the allowed codes; never a silent fall-back to all) | `time_range, platform (all \| entitled code incl. copilot), topic_ids[], competitor_ids[] (get_competitor_list rows' legacy id)` |
| `list_citation_domains` | **The domain TABLE of `/sources/citations`** (0.6.0, same read model as the page): one page of cited root domains with `rank`, `rootDomain`, `domainType`, `citations` (incl. today), `share` (of all citations in the window — denominator ignores filters), `prevCitations` / `deltaPct` / `isNew` (closed days vs previous window; null = no comparison), `selfMentioned` (answers citing this domain also mentioned you; null = co-occurrence layer off → "unknown"), `brands[]` + `brandsExtra` (registered competitors mentioned in answers citing the domain). **`gap_only=true` = the page's "只看缺口" switch** (competitor mentioned, you not, not a competitor's own site) — `gapUnavailable="no-competitors"` means none registered, not "no gaps". `status="not-ready"` / `invalidFilter="competitors"` / `coverage.missingDays` are states to report. One domain's pages / answers → `get_domain_detail`. `platform`: entitled code or error (same rule as `get_citation_overview`) | `time_range, platform (all \| entitled code incl. copilot), topic_ids[], competitor_ids[], search, gap_only (bool), type: all\|you\|competitor\|other, sort: citations\|delta\|domain\|share, page, page_size (default 20, max 100)` |
| `get_domain_detail` | One domain's citation profile — the drill-down page `/sources/citations/<root_domain>`: `totalCitations`, `share` (raw 0–1 = domain ÷ all brand citations in the window; the page KPI shows `(share × 100).toFixed(1)`), `citationShare` (same ratio as a 2-decimal percentage, old callers), trend, pages, prompts, platforms, regions — `totalCitations` / `share` match the drill-down KPI, but `pages[]` is the raw per-URL grouping (still includes the search / redirect links the page's table drops, and has no mentions-you / last-cited / delta columns). Same `caliber` + `window` contract as `get_citation_overview` (+08 calendar days on citation-creation axis + today). `is_subdomain=false` + a registrable root matches the whole root; `is_subdomain=true` or a non-root input matches that exact host. `platform`: entitled code or `{ error }` | `domain, is_subdomain (bool), time_range, platform (all \| entitled code incl. copilot)` |
| `get_page_detail` | One page URL's citation detail: `totalCitations`, `share` (raw 0–1 — format with one decimal like the rest of the citations surface), `citationShare` (2-decimal %, old callers), trend, prompt distribution, text snippets. Same `caliber` + `window` contract as `get_citation_overview` (the **window** is the `/sources/citations` one); matches by **lookup URL** (query string / trailing slash dropped). ⚠️ **Not the pages-tab row** — lookup-URL grain vs the table's raw url, and `share` divides by *all* brand citations while the table's total drops search / redirect links; quote it as this tool's figure, not as the page row. `platform`: entitled code or `{ error }` | `url, time_range, platform (all \| entitled code incl. copilot)` |
| `get_url_reference_detail` | A URL's references across **both** citations and ChatGPT search sources; arbitrary windows. **Tool-only caliber** (no page has this view): lookup-URL grain + a **rolling** window (`now − N×24h` or the custom `[start_date, end_date)` instants) — `citationReferences` is NOT the `/sources/citations` pages-tab row (exact URL, whole +08 calendar days); the closest page-window tool is `get_page_detail`. `platform`: entitled code or `{ error }` (was: silent all-zero result) | `url, time_range: 7d\|30d (default 30d — no 90d), start_date, end_date, platform (all \| entitled code incl. copilot), prompt_id, limit_recent (1–100)` |

---

## E. Brand-own — competitors, topics, platform, sentiment (read-only)

| Tool | Purpose | Params |
|---|---|---|
| `get_competitor_list` | The brand **library** — same rows as Settings › Brand › Brands (brand-entity layer, not the legacy competitor table). **Paged envelope** (0.6.0): `{ rows[], total, page, page_size, total_pages, counts {tracked, suggested, removed}, names_mode }`; rows carry `status: tracked\|suggested\|removed`, tracked rows include your own brand (`is_own_brand=true`). Competitors = `status="tracked" AND is_own_brand=false`. Fields: `entity_id`, `name`, `root_domain`, `mentions_30d` (rolling 30d, entity rollup), `names[] {name, user_added, is_distinct}` (`is_distinct=false` = a variant the page's name column folds away — a spelling containing a shorter spelling of the same entity word-for-word; with `entity_id` + `names=full` the fold is computed over this page's spellings plus your user-added ones, not the whole dictionary, and capped at 601 spellings = the page, the entity's 100 shortest user-added spellings looked up independently of the page window, and the own-brand primary name which is always kept) + `names_total` / `names_truncated` (default `names=summary` = user-added spellings + up to 12 learned ones, distinct first — pass `entity_id` + `names=full` for one entity's complete alias list, 500 per call via `names_offset` / `names_next_offset`), `primary_name` (own-brand row: the locked brand-name spelling); `sort` / `order` replay the page's column sort inside each status group with the own-brand row pinned first (omit = page default order); envelope `rescan_enabled` (see `create_competitor`; `false` = library edits apply from the next collection run and existing answers are re-counted in a later maintenance pass); legacy `id` (competitor row id, null unless user-added) / `domains` / `aliases` / `isActive` kept one release, mapped from the entity layer | `status: all\|tracked\|suggested\|removed (default all), sort: mentions\|name, order: asc\|desc, search, entity_id, names: summary\|full\|none (default summary), names_offset, page, page_size (default 50, max 200)` |
| `get_brand_board` | **Entity-caliber brand board.** No `topic_ids`/`country` → `mode=confirmed`, `caliber=brand_entity_v1` — **this is the in-app `/performance` board, value for value**: rows = **confirmed competitors** (manual + auto slots → terminal entities); `visibility` = answers mentioning the entity ÷ completed answers (pooled over covered days — **same formula for your brand**, returned in `ownBrand` with `share`, `rank`, `pending`); `share` = mentionedRecords ÷ (you + all confirmed competitors). `include_trend=true` adds the page's **trend chart**: `trend[]` per UTC+8 business day (`completedRecords`, `brandMentionedRecords`, `brandVisibility`, top-4 competitor lines) + `trendCompetitors` (always 4, independent of `limit`) — raw numerator/denominator included, so re-bucket by week/month from those, never by averaging daily percentages. **The trend is not one point per day of the window**: days before `coverage.floorDay`, mid-window days the counting lane has not processed, and the last 1–3 days under 90% counted are dropped whole — so **yesterday is often missing**; read `trendDays` / `trendFirstDay` / `trendLatestDay` (a gap to the window end = "not counted yet", not "no data") and `trendBasis`. Long windows are capped to fit the output budget with `trendTruncated=true` — the **newest** days are kept, the oldest dropped. When `ownBrand.pending=true`, `trend[].brandVisibility` / `brandMentionedRecords` are **`null` on every point** (never 0 — the page hides the own-brand series in that state); only the competitor lines are real. With `topic_ids`/`country` → `mode=scoped_open_world` and a **different, tool-only caliber** (`caliber=scoped_open_world_v0`): only the row source matches the in-app filtered board, **the numbers do not** — rows = the exact entities mentioned inside that scope (ranked by `mentionedRecords`), `pooledMentionRate` (= `visibility` alias) = mentionedRecords ÷ completed records in scope, all platforms (null under a `platform` filter — the row source ignores platform), `candidateShare` (= `share` alias) = share within the candidate set, capped at the top 200 entities by `mentionedRecords` (same cap as the page), so `candidateTruncated=true` ⇒ share values are upper bounds and `total` is the candidate-set size, not the number of entities in scope (narrow the scope for an exact share). The in-app board under a filter still renders the **legacy merged board** (old day-averaged visibility + old SoM matched by name, legacy-only rows added back, sorted by visibility) and its trend chart falls back to the legacy caliber too (`include_trend` is ignored there) — never present scoped numbers as the page's filtered board, and never compare them with `mode=confirmed` rows. Filtered mode is **fail-closed** — `status=not-ready` when the derived layer is not ready, never a silently unfiltered board. `status=no-coverage` = entity counts not computed for the window yet (not "no mentions"). Not the discovered-brand caliber of `get_competitor_overview` — don't mix them | `time_range (7d\|30d\|90d\|180d\|custom, default 30d), start_date, end_date (custom; span ≤ 90d), platform, topic_ids[] (1–50 ids, each id 1–64 chars; `_none` = untopic'd prompts), country (ISO2, ≤8 chars), limit (1–50, default 20), include_trend (default false, confirmed mode only)` |
| `get_competitor_cooccurrence` | Brand + competitor co-occurrence (brand mentioned AND ≥1 other brand entity in the same answer): per-record detail + summary; optional answer text. **Caliber (2026-09-20)**: competitors come from the brand-entity layer — the same read path as the `/performance/answers` "mentioned brands" column (`brand_entity_mention_shadow` counted rows folded to their entity + per-answer `stance` from `brand_mention_vote`; per-answer fallback to `prompt_record_mentioned_brand`), NOT the legacy `prompt_record.competitors` JSON. Each competitor: `entityId, name, mentions, stance (preferred\|equal\|inferior\|we_absent\|null), majorityRole`. The brand's own entity and channel-role entities (retailers, app stores, media) are dropped **only where the entity layer supplies a role** — answers that fell back to `prompt_record_mentioned_brand` carry no role, so channel names can appear there; those rows have `entityId` prefixed `prmb:`. Summary on the newest 2000 mentioned answers, plus `arm` (entity\|prmb) and `truncated` (row fuse hit → competitors left empty; report it, it is not "no competitors"); `summary.byPlatform[]` carries both `platformCode` (what you pass as `platform`) and `platform` (display name). ⚠️ `rows[].sentiment` is the LEGACY record-level label (the `get_sentiment_dashboard` caliber), **not** the verdict aspect polarity and not the per-competitor stance — that is `competitors[].stance`. Unknown platform code → error (it used to return silently empty) | `time_range, platform (all\|chatgpt\|perplexity\|gemini\|google_ai\|copilot\|google_ai_overview), limit (1–100), include_answer (bool), answer_max_chars` |
| `get_competitor_polarity` | **Who beats you** — the AI Verdict (`/performance/verdict`) competitor board, same read model as the page (2026-09-20): per linked competitor entity, `weLose` = answers in the window where the model voted `stance = preferred` for that rival (`brand_mention_vote`, one vote per answer × entity, current analyzer generation, entities folded along redirect chains). Rows under 3 answers dropped; top 5 by `weLose`; `delta` / `isNew` vs the previous equal-length window. Votes exist only for answers analyzed **since 2026-09-08**. No `tie` / `weWin` / `netLoss` / `coMentions` any more (dropped with the page redesign). Returns `{ window {start,end,days,label} (business days), platform, topicIds, ready, generation, builtAt, note, minRecords, standings[{entityId,name,domain,weLose,delta,isNew}] }` — exactly these nine top-level keys. `note` is normally `null`; non-null = the rollup published a new generation mid-read (or the pointer read failed), so `generation` / `builtAt` may not describe the same moment as `standings` and the response was not cached — retry in a minute for a stable snapshot. `ready=false` = the brand has no perception rollup pointer, i.e. the in-app page shows an EMPTY board right now even if `standings[]` is not empty (the tool reads the vote table directly) — say so rather than quoting numbers the user cannot see (an empty `standings[]` = nobody reached `minRecords`, not a failure; errors come back as `{ error }`). Polarity, **not** visibility — pair with `get_platform_matrix` | `time_range (7d, 30d, custom — custom requires start_date), start_date, end_date, platform (all\|chatgpt\|perplexity\|gemini\|google_ai\|copilot\|google_ai_overview; must be entitled, else error), topic_ids (≤50 prompt_topic ids = the page topic chips; each id trimmed, then 1–64 chars)` |
| `get_platform_matrix` | Comparison matrix: brand + **automatically discovered** competitors × platform (overall Top 5 by `metric` + the brand), or topics × platform. Cell `somShare` = records-based Share of Mentions WITHIN that platform column; cells also carry `mentionedRecords`. Competitor rows come from the brand-entity layer (`brand_entity_mention_shadow`, `count_state = counted` only — ambiguous surfaces without a confirming vote are **not** counted, see metric-calibers.md). This is the competitor visibility tool for new work (`get_competitor_overview` is deprecated — same query, legacy flat shape, see the Deprecated section) | `dimension: topic\|competitor (default competitor), time_range, platform, metric: aigvr\|som\|citation (default aigvr)` |
| `get_topic_analytics` | Per-topic answer analytics — same read model as a topic detail page's sentiment / response-type / competitor cards. `byTopic[topicId]` = `sentimentDistribution`, `avgSentimentScore`, `responseTypeDistribution`, `topCompetitors` (the **other brands named in the same answers**; with the brand-entity read on these are resolved entities, so aliases fold together, vetoed brands drop out and your manually tracked competitors appear alongside the auto-discovered ones — it is not a report on your competitor list), `recordCount`, `analyzedCount` (the cards' denominator — 0 means nothing analysed). Topic-less prompts are keyed `_ungrouped` and are **in by default**. `global` adds `platformDistribution` + `visibilityTrend` / `intentTrend`, but **no page shows `global.*`**. Calibers: both trends are **weekly** buckets (Asia/Shanghai week start), not daily; `time_range="all"` is floored to a trailing 30d unless `start_date` is given; the competitor arm reads the T3E derived layer only when `platform="all"` and it is ready, else it computes the same numbers live. Brand-wide sentiment is `get_sentiment_dashboard` — different caliber, the two will not add up | `time_range: 7d\|30d\|90d\|180d\|all\|custom (default 30d; custom requires start_date), start_date, end_date, platform, topic_ids[], include_ungrouped (bool — only meaningful WITH topic_ids: widens that subset to also admit topic-less prompts. Passing it WITHOUT topic_ids narrows the scope to ONLY topic-less prompts)` |
| `get_sentiment_dashboard` | **NOT the `/performance/verdict` caliber** — that page counts aspect-level votes (`brand_mention_aspect_vote`); for it use `get_competitor_polarity` / `get_risk_context_sources`. This is the legacy RECORD-LEVEL label (one `prompt_record.sentiment` per answer, no page renders it) and its totals do not reconcile with the verdict page. Brand-wide sentiment over a rolling window: `overview` (positive / neutral / negative / mixed counts, `total` = every record that carries a sentiment value — normally their sum, can exceed it if an unrecognised value ever appears, `avgScore` −1..1), `trend` (per UTC+8 business day, gaps = no run), `byPlatform` (per platform, `positiveRate`). **No longer returns** `highlights` / `brandCorrelation` / `positionAnalysis` / `platformEnhanced` (2026-09-20) — verbatim evidence → `get_brand_mention_samples`, polarity → `get_competitor_polarity`. `source` echoes `derived\|live` (same numbers). Unknown platform code → error | `time_range: 7d\|30d\|90d\|180d, platform (all\|chatgpt\|perplexity\|gemini\|google_ai\|copilot\|google_ai_overview)` |
| `get_risk_context_sources` | **Cited sources** of the AI Verdict page (`/performance/verdict › Sources` tab), same read model as the page (2026-09-20): per domain cited by answers that carry aspect votes in the window — `kind` (owned\|review\|community\|media\|rival\|commerce\|reference\|other), `citedRecords`, `negativeShare` (share of those answers where the brand's **own** entity got a `negative` aspect vote), `topAspects` (≤2 objects `{aspectId,label,labelZh}` — `aspectId` is the page AspectChip id, pass it back as `aspect_id`), `delta`, `isNew`; plus `kpis` (`domains`, `domainsDelta`, `ownedRecords`, `ownedDomain`, `worstDomain`, `newDomains`), `domainsTotal` and `filtersApplied` (`topicIds`/`aspectId`/`kind`/`q` as actually applied). Window follows `time_range`; ⚠️ **default 7d, while the page tab (and `get_competitor_polarity` on the same screen) defaults to 30d** — pass `time_range=30d` to reproduce the page, and never put a default call of this tool next to a default call of `get_competitor_polarity`. The 7d default is a cost decision (30d cold measured 50.9s / 81.2s on two large brands, past the 45s budget), not the page caliber. ⚠️ `kind` / `q` narrow the aggregated domain list, so every KPI and `domainsTotal` follow the filter and `kpis.domainsDelta` is `null` while either is set. No `lift` field any more; aspect votes exist only since 2026-09-08. Co-occurrence, not causation | `time_range (7d, 30d, custom; default 7d — the page uses 30d), start_date, end_date, platform (all\|chatgpt\|perplexity\|gemini\|google_ai\|copilot\|google_ai_overview), topic_ids (≤50, each id trimmed, then 1–64 chars), aspect_id (trimmed, 1–64 chars), kind (owned\|review\|community\|media\|rival\|commerce\|reference\|other), q (≤120 chars)` |
| `get_brand_mention_samples` | Sample recent answers mentioning the brand (`mentions > 0` on completed records — the `/performance/answers` "mentioned you" predicate): raw text + sentiment + extracted context; rows carry `recordDate` and `businessDay` (UTC+8 `YYYY-MM-DD`, quote this one). Unknown / un-entitled `platform` → error object (2026-09-22; was a silent empty sample) | `time_range, platform (all \| one entitled code incl. copilot), limit (1–30), answer_max_chars (200–4000)` |

---

## F. Brand-own — audits & site traffic (GA4 / Cloudflare, read-only)

| Tool | Purpose | Params |
|---|---|---|
| `get_audit_list` | GEO site audits (AI-readiness diagnostics), paginated history — same rows as the /audit page. `auditMode` decides what the row carries: **site** rows have the three sub-scores `null`; **single** rows have `criticalCount` / `warningCount` = 0 meaning *not computed* (use `get_audit_detail.headline`). `status` ∈ pending / running / completed / failed — pending/running are still scanning, poll later | `page, page_size (1–100)` |
| `get_audit_detail` | One audit = the /audit/[id] report (same read models as the page, 2026-09-22). Always the audit row; when `completed` also the report header — `headline {fail, warn, pass}`, `dimensionScores[]` (D1 / D2 / D3 / D8 rings, page formula per mode), `citationInsights` (the "AI citation insights" banner: this brand's monitoring citations of the audited domain, rolling 30 days) — **opt-in, `include_citation_insights=true`**: it is a whole-history scan, so by default it comes back as `{unavailable: true, reason: "not_requested"}` and the rest of the report stays a point read; when asked for, it is cached 1h per brand + domain + day and can still be `{unavailable: true, reason: "budget" | "timeout" | "error"}` — none of which means "zero citations". For plain per-domain citation counts `get_domain_detail` is the cheap read. **single** mode: `checks[]` (every check run on that page, with `confidence` / `dataQuality`) + `page` header; `aggregatedIssues` is `null`. **site** mode: `aggregatedIssues` (critical/warning/passed, each capped ~25 with `_counts`), `pageTypeStats`; per-page results via `get_audit_pages`. `checksMeta` maps `checkId` → `problemTitle` / `problemDescription` / `howToFix` / `severity` / `effort` / `isQuickWin` (the copy the page renders); it covers the ids of the FULL report, so in site mode also the ones past the ~25-per-bucket cap | `audit_id, include_citation_insights (bool, default false)` |
| `get_audit_pages` | Per-page results of one audit — the "By page" tab and the unusable-pages list. Each item: `url`, `pageType`, `score`, `status`, `fetchQuality` (high / medium / low / unusable) + `fetchIssues[]`, `counts {fail, warn, pass, na}`, `checks[]`. Oldest first, stable paging; resolve check ids via `get_audit_detail.checksMeta` | `audit_id, page, page_size (1–25), page_type (homepage \| product \| collection \| article \| faq \| brand \| robots \| sitemap \| llmsTxt \| other), fetch_quality (high \| medium \| low \| unusable)` |
| `get_ga4_traffic_data` | GA4 integration, same read model as `/sources/traffic` › GA4 tab. **Two denominators in one payload**: `metrics` / `changes` / `timeSeries` / `landingPages` / `channelDistribution` are WHOLE-SITE; `aiSources`, `totalAiSessions`, `aiTrafficShare`, `platformLandingPages`, `aiTrend`, `transactionsTrend`, `revenueTrend`, `platformDurations`, `aiDevice/aiCountry/aiNewVsReturning` cover only AI-referred sessions. The page renders neither `landingPages` nor `timeSeries` (its table is `platformLandingPages` top-25, its chart is `aiTrend`), and its channel chart subtracts `totalAiSessions` from `Referral` and shows it as a synthetic "AI Traffic" channel — `channelDistribution` here is raw GA4. Tool default 30d, **the page opens on 7d**. Pass `page_path` for **page-level** data instead (page views, sessions, bounce rate, engagement, conversions, traffic sources incl. AI sources, daily trend). `platforms` / `landing_page_limit` narrow the payload the way the page does; totals stay whole | `property_id (optional), time_range: 7d\|30d\|90d\|custom, start_date, end_date (custom), comparison_mode: previousPeriod\|previousMonth\|previousYear, page_path (optional — page-level mode), platforms[] (optional), landing_page_limit (optional, no default)` |
| `get_cf_traffic_data` | Cloudflare **AI-crawler** traffic (bots fetching your pages — not referral traffic), same read model as `/sources/traffic` › Cloudflare tab: `aiCrawlerRequests` / `totalRequests` / `totalBotRequests`, `changes` (`aiCrawlerRequests` / `crawlerCount` vs the previous equal-length period; **`changes.blockedEvents` is always `null`** — the firewall side has no previous-period query, so that null is "never computed", not "no blocking last period"), `crawlers[]` (`topPaths` / `countries` top 5 each), `crawlerPathBreakdown[]` (Sankey), `topCrawledPaths[]` (**top 10** — the read model's own cap), `dailyTrend[]`, `countryDistribution[]` (top 10), `blockedCrawlerSummary[]` (**every** blocked AI crawler, uncapped — the in-app card shows its first 10) + `blockedEvents[]` (capped by `blocked_events_limit`, default 50 = the in-app table, out of the ≤2000 the read model collects; `blockedEventsTruncated {returned,total}` appears when rows were dropped; order = days oldest→newest, newest-first within a day), `maxDays` (the zone's CF plan retention — the window is clamped to it), and `degraded.previousPeriod` / `degraded.blockedEvents` (a fetch FAILED ⇒ nulls/empties are "could not look", not "nothing happened"). Default **7d** (the tab's own default; CF is queried one batch per day). Not connected ⇒ `{ success:false, error:"CF_NOT_CONNECTED" }` | `time_range: 7d\|30d\|90d\|custom, start_date, end_date (custom), blocked_events_limit (1–2000, default 50)` |

---

## G. Brand-own — write tools (require consent **write** grants)

| Tool | Purpose | Params |
|---|---|---|
| `create_prompt` | Create a new monitoring prompt | `text, category, priority, country (default US), topic_id, tags[]` |
| `archive_prompt` | Archive a prompt (stops monitoring, cancels in-flight collection, moves it to the Archived tab) or restore it with `restore=true`; idempotent (`changed=false` when already in that state). Read archived ones with `get_prompt_list status=archived` — no separate read tool | `prompt_id, restore (bool, default false)` |
| `update_prompt_tags` | Batch tag edit, same as the /prompts bulk actions: `add` / `remove` tags on a set of prompts (≤500), or `rename` one tag brand-wide. Foreign prompts are skipped. Tags are read back via `get_prompt_list` rows | `action: add\|remove\|rename, prompt_ids[], tags[], old_name, new_name` |
| `move_prompts_to_topic` | Move prompts (≤500) into a topic of this brand, or `topic_id=null` to ungroup — the /prompts "move to topic" bulk action | `prompt_ids[], topic_id (nullable; from get_topic_list)` |
| `create_topic` | Create a prompt topic (unique within brand, case-insensitive) | `name, description` |
| `create_competitor` | Add a brand to the brand **library** — the same "add brand" path as Settings › Brand › Brands (0.6.0). A name that already belongs to a suggested / removed brand **re-tracks that entity** (`adopted_existing=true`); rejects your own brand name (`is_own_brand`), invalid names/domains, a domain held by another tracked brand (`domain_taken`). Calling it for a brand that is **already tracked is not an error**: it is the idempotent top-up path — `outcome="already_tracked"` (no second brand is minted, `domains` ignored), the aliases you pass are attached to that existing brand (`already_present` for ones it has) and the legacy alias column is re-written, which is how a failed legacy write is repaired; `already_exists` is left only for a name that collides with a tracked brand that cannot be located as an entity. Only `domains[0]` (≤20 domains, each ≤253 characters) becomes the entity `root_domain`; `aliases[]` (≤20) are attached **one at a time** afterwards under the page's **no-steal rule** (a spelling owned by another entity ⇒ `name_taken`, the brand is still created) — every alias comes back in `spellings[] {name, result}`: `added` \| `already_present` \| `duplicate` \| `name_taken` \| `invalid_name` \| `entity_unavailable` \| `failed` \| `not_attempted` (attach budget spent — the brand and the earlier spellings are saved; retry with just those). The whole write path (brand row + every attach + the legacy alias write-back) shares one ~18s deadline and every transaction caps its statements at 2s DB-side — past it nothing new starts (`deadline_exceeded` on the brand step, `not_attempted` on the rest), so a timed-out call leaves at most ONE spelling write finishing in the background; repeating the call is idempotent. The legacy `data.aliases` row column gets the spellings that actually landed (`added` + `already_present`), never the refused ones — that mirror runs **outside** the create transaction, so the response self-reports `legacy_aliases_synced`: `false` means the legacy column write failed (the brand and its spellings are still saved in the library), `data.aliases` then reports what that column **actually** holds instead of what was attached, and `note` tells you to retry the same call — that retry now really does repair it (it lands on the `already_tracked` path and re-runs the merge write, nothing is duplicated) — and that `/sources/citations` owned-vs-rival classification is behind until it succeeds. A brand that lives in the entity layer only (system-discovered, never a legacy competitor row) comes back with `data: null` and `legacy_aliases_synced: false` plus a note saying a retry will not change that. A call that attaches no new spellings (none passed, or all refused) writes nothing and `data.aliases` reports the column's current contents. Returns `entity_id`, `outcome` (`created` \| `adopted` \| `already_tracked`), `status="tracked"`, `rescan_enabled` + `effective_from` (`now` \| `next_collection` — with `next_collection` the change applies from the next collection run and existing answers are re-counted in a later maintenance pass, they are **not** left uncounted), plus the legacy `data` row | `name, domains[] (≤20), aliases[] (≤20)` |
| `trigger_prompt` | Run monitoring for a prompt now (**consumes credits**) | `prompt_id` |

---

## H. Report tools (user-scoped, no brand gate)

| Tool | Purpose | Params |
|---|---|---|
| `get_agent_ready_scans` | List Agent Readiness scan history for the token owner | `limit (1–100, default 20)` |
| `get_agent_ready_scan_detail` | Full Agent Readiness scan result by ID — `summary` is the page's score card (overallScore over the 4 main categories, commerce excluded; always on the unfiltered checks), each check carries the page's `label` / `goal` / `howToImplement` / `specUrls` / `skillUrl`, plus `shareUrl` | `scan_id, preset (all/content/api), checks[], include_evidence (default true)` |

---

## I. Public source domains (read-only, available to all tokens)

These read cross-topic public citation-source data and need no brand context (NOT Grow-gated).

| Tool | Purpose | Params |
|---|---|---|
| `get_public_sources_overview` | Most-cited public source domains across all topics (cross-topic leaderboard). Each row's `score` = **AI DA** (0–100 AI Domain Authority: citation share + placement + breadth). Calibers (same read model as the /industry/sources list): the whole payload is **one platform's** rollup — numbers from two platforms are never additive; `share` denominator = all usable citations of that platform globally and a `type` filter narrows the ranked rows only, never the denominator or the header totals; `lastRefreshed` is the rollup's own coverage watermark (may be null / lag the live lists — never render it as “latest”); `integrityStatus` review\|restricted is a neutral data-quality flag, not a score penalty | `limit (1–200), type, platform (default chatgpt)` |
| `get_public_source_domain_detail` | Profile one citation source domain with the **same read models as the /industry/sources/[domain] page**: usable citations, `aiDa` (the page reads it as a PR tier: **≥80 core / ≥60 worth / ≥40 watch / <40 skip**), `categoryCount` (the page's third hero KPI), `keyRead` (`categoryFocus` — vertical (top category ≥20% of classified citations AND ≥1.5× the runner-up) \| broad (no dominant category) \| unclassified \| insufficient (<50 classified citations) — plus `topBrands`), `categoryStanding` (weekly usable-citation share trend over `period`; with `category_slug` also this domain's rank / poolSize / share **inside** that category), `topicCoverage` (first page ≤20, all-history, with `dominance` + `stronghold` (= dominance ≥ 0.15, the page's label) + `trend`), `coOccurringBrands` (first page ≤20), `topUrls` (first page ≤20 — **since 2026-09 windowed by `period` 30d (default) \| 90d** anchored on the domain's latest usable citation day, like the page; was: all history — each row carries `archetype` comparison\|review\|guide\|question\|other). Each list carries `page/pageSize/hasMore/total` under `lists`; follow `hasMore` with `list=` + `page=` (+ `topic_search=`), which returns **only that list plus the header** (plus `categoryStanding` when you also pass `category_slug`). `page` **only applies with `list=`** — without it all three lists stay on page 0. Calibers: headline counts come from the last sealed batch (may trail live lists ~1 day); `coOccurringBrands` only counts pairs with **≥10 co-occurring answers within one topic** (absent ≠ zero) and is a **different population** from `keyRead.topBrands` (domain-level rollup, no per-topic threshold, no answer-text filter — but the same **≥10 co-occurring answers** admission applies at the domain level, so absent ≠ zero there too) — the two can disagree; `dominance`/`stronghold` are computed for the **first 12 rows of page 0 only** (null beyond = not computed, **not zero**; the page files null under “in the mix”) — **searching does not disable them**: a searched page 0 still scores its first 12 matching rows; `archetype` is a heuristic over the page **title**, English only; `totalAppearances`/`usableRate` are deprecated, always null | `domain, include_scorecard (bool — adds the full AI DA scorecard: authority/placement/breadth sub-scores, rank + pool size, momentum, confidence, integrity), period: 30d\|90d (default 30d — drives topUrls **and** categoryStanding.trend), category_slug, list: topics\|brands\|urls, page (0–25, needs `list`), topic_search (topic map), platform (default chatgpt)` |
| `get_public_source_brand_conduit` | The topics where one source domain's citations co-occur with one brand (top 30) — the contexts in which this source funnels AI toward that brand. Same ≥10-per-topic threshold: empty = nothing **above** the threshold, not strictly zero | `domain, public_brand_id, platform` |

---

## J. Public / industry tools (29) — Grow tier and above

The cross-brand competitive-intelligence surface (multi-org: enabled when any accessible org
qualifies). Full playbook + locale/platform conventions
in [public-tools.md](./public-tools.md). Most data tools accept an optional `platform` (default
`chatgpt`) — discover valid platforms per scope with `get_available_platforms`. **Time window
(since 2026-09)**: the windowed public tools default to the **latest published 30d batch
window** the web pages use (was: all history); every windowed tool takes `range: 30d|60d|90d|all`
and explicit `date_from`+`date_to`, and echoes the resolved `window`. Get the anchor first with
the free `get_public_data_window` — details in public-tools.md § Time window convention. Quick
index (26 current entry points + 3 public deprecated aliases from § Deprecated = 29 registered here; the other 4 deprecated names, `get_competitor_overview`, `get_brand_citations_daily`, `get_ga4_page_data` and `get_content_opportunities`, are brand read-only tools counted in the 41 — 7 deprecated names in total):

**Resolve & browse**: `get_public_data_window` (free — batch anchor + 30/60/90d windows, and `brand_id` → the
`/brand/[id]/report` brand-high-water-mark anchor),
`search_public_entities`, `list_public_topics`, `list_public_locales`, `get_available_platforms`
**Topic**: `get_public_topic_overview`, `get_public_topic_brand_leaderboard`,
`get_public_topic_som_trend`, `get_public_topic_prompt_matrix`, `list_public_topic_prompts`,
`get_public_topic_prompt_detail`, `get_public_topic_record_detail`,
`get_public_topic_citation_domains`, `get_public_topic_commerce`,
`get_topic_competition_difficulty`
**Brand**: `get_public_brand`, `get_public_brand_rank_citation` (AIO-only Rankings × AI Citations), `get_public_brand_perception`
(mode=`profile` | `aspect_mentions` — the per-aspect drill-down), `compare_public_brands`
**Category / product-space**: `get_public_category`, `get_category_whitespace`,
`get_category_brand_momentum`
**AI-search intelligence**: `get_public_search_queries` (incl. mode=`territories` — demand-root
ownership map; mode=`query_detail` / `theme_detail` — one query / one topic drill-down)
**Shopping**: `list_public_shopping_boards` (cross-category AI shelf: hot/climbers/entrants,
batch-vs-batch, `page` × `limit` down to 200 deep, echoes `data.platformUsed`),
`get_public_shopping_product_detail` (one product's full analysis; mode=`card` for the cheap
preview; merged ids follow the survivor; `availablePlatforms` per product),
`list_public_shopping_products` (one category's shelf; `search` / `channel_domain` drill-downs).
Probe platforms for these with `get_available_platforms scope=shopping` / `scope=product`
(shelf rows, not answer records).

---

## Count by registration source (max surface)

Counts are the largest configuration (single-org token + write grants + Grow-tier+; multi-org
adds the 2 selectors but clamps the 7 write tools). A brand-bound, read-only, lower-tier token
sees far fewer.

| Source | Count |
|---|---|
| Read-only (brand + GA4/Cloudflare + public-source + `get_brand_context` + `get_current_date` + `resolve_page_context`) | 41 |
| Discovery selectors (`list_brands`, `list_organizations`) — multi-brand/org only | 2 |
| Write (consent write grants) | 7 |
| Report (user-scoped) | 2 |
| Quota (`get_quota`, always registered) | 1 |
| Public / industry (Grow+) | 29 |
| **Max total** | **82** |

> `get_discovered_links` is in `MCP_EXCLUDED_TOOLS` (its source tool `fetch_page` is in-app
> only) and is **not registered at all** — it is not part of the 41. Display sections A–J above
> regroup these for navigation; `get_brand_context`, `get_current_date` and `resolve_page_context` live under both
> "discovery" (display) and the read-only count (source). The counts include the deprecated
> aliases below (they are still registered).

## Deprecated (still registered)

These names still exist on MCP so existing integrations keep working — each one forwards to its
parent tool and returns exactly what it always did, at the same price. Their descriptions start with
`[DEPRECATED → <parent>]`. **Do not pick them for new work**; call the parent with the
equivalent arguments instead.

| Deprecated tool | Call this instead |
|---|---|
| `get_competitor_overview` | `get_platform_matrix` with `dimension="competitor"` (same query, same price; response shape of the alias unchanged) — see docs/mcp/COMPETITOR_TOOLS_CALIBER_AUDIT_2026-09.md §1.3 |
| `get_brand_citations_daily` | `query_analytics` with `dataset="brand_citations_daily"`, `dimensions=["date","platform"]` (+ `"platformName"`), full metrics list (see § query_analytics recipe). Its rows inherit the parent's 2026-09-22 caliber fixes: `date` is the **UTC** day key (= in-app business day − 1) and the daily rates carry 1 decimal (they used to be whole numbers); the alias takes no `topic_ids` / `country` / `businessDate` — call the parent for those |
| `get_ga4_page_data` | `get_ga4_traffic_data` with `page_path` |
| `get_content_opportunities` | `list_citation_domains` with `gap_only=true` — the page section this tool mirrored ("content opportunities") was removed in the 2026-09-04 citations redesign and replaced by the gap switch. **Not a forwarding alias**: the old name keeps its old semantics (prompts where a given domain has low/no citations, legacy competitor text column) and price; it is simply no longer the page caliber |
| `get_public_brand_perception_aspect_mentions` | `get_public_brand_perception` with `mode="aspect_mentions"`, `aspect=<normalized_label>` |
| `get_public_search_query_detail` | `get_public_search_queries` with `mode="query_detail"` (`query` + `product_space_id`) or `mode="theme_detail"` (`topic_id`) |
| `get_public_shopping_card_detail` | `get_public_shopping_product_detail` with `mode="card"`, `product_space_id` (days 1–90 there; the alias still accepts 1–180) |

### Retired facets / modes (still served, with a `_deprecated` notice)

Retired on 2026-09 under the three-step rule (mark → keep a compatibility forward for one
release → delete). They still answer for now but carry a `_deprecated` notice in the response
and will be removed in the next skill release — **do not start new work on them**:

| Tool / facet | Why | Use instead |
|---|---|---|
| `get_public_search_queries` mode=`queries`, `themes`, `brand_landscape`, `prompt_map` | The /industry/search page moved to a global batch-anchored model on 2026-07-23; these all-history readers have no page equivalent | mode=`territories` (demand ownership), `get_public_search_query_detail` (one query), `get_public_category` view=`topics`/`brand_leaderboard`, `list_public_topic_prompts` + `get_public_topic_prompt_detail` |

### Removed facets (no longer accepted)

| Tool / facet | Removed | Use instead |
|---|---|---|
| `get_public_brand` / `compare_public_brands` view=`exposure_quality`, `segments`, `topic_flow`, `presence_trend` | 2026-09 (step ③ of the three-step retirement — the enum now rejects them) | `visibility` (+`include_trend`), `competitors`, `footprint` on the same window; diff two windows yourself for movement |

## Not on MCP (in-app agent only — do not call these over MCP)

These exist in the codebase but never reach MCP — either listed in `MCP_EXCLUDED_TOOLS` or
defined outside the read-only registration set (in-app agent / audit flows only):
`web_search`, `fetch_page`, `get_discovered_links`, `display_data`, `display_chart`,
`report_page_analysis`, `generate_site_report`, `get_analysis_reports`, `get_report_detail`,
`save_analysis_report`, `create_action_tasks`. Calling them over MCP returns "unknown tool".
